name:
category:
info:
inputs:
  - info:
    value:      #optional
    editable:   #optional
outputs:
  - info:
    value:      #optional
vars:
  - dtype:
    name:
help:
code: # Either a block of code or a path to a .c file
  block:
  path: